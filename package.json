{
  "scripts": {
    "test": "mocha ./lib/**/*.tests.js --compilers js:babel-core/register",
    "test:watch": "mocha ./lib/**/*.tests.js --compilers js:babel-core/register -w",
    "build": "babel src -d lib"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-register": "^6.24.1",
    "chai": "^4.0.1",
    "mocha": "^3.4.2"
  },
  "name": "filter-appear-once",
  "version": "0.0.1",
  "description": "Returns all strings that only appear once in two arrays",
  "main": "filter-appear-once.js",
  "author": "Marc Nitzsche",
  "license": "MIT"
}
